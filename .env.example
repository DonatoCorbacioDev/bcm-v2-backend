# =================================================================
# BUSINESS CONTRACTS MANAGER - Environment Variables Template
# =================================================================
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file as .env in the same directory (bcm-backend/)
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit the .env file to Git (it's in .gitignore)
# 
# =================================================================

# =================================================================
# DATABASE CONFIGURATION
# =================================================================
# MySQL Database Connection
# Create your database first:
#   mysql> CREATE DATABASE bcm CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
#   mysql> CREATE USER 'your_user'@'localhost' IDENTIFIED BY 'your_password';
#   mysql> GRANT ALL PRIVILEGES ON bcm.* TO 'your_user'@'localhost';
#   mysql> FLUSH PRIVILEGES;

DB_URL=jdbc:mysql://localhost:3306/bcm
DB_USERNAME=your_database_username
DB_PASSWORD=your_secure_database_password

# =================================================================
# JWT CONFIGURATION
# =================================================================
# Generate a secure JWT secret key (minimum 256 bits / 32 characters)
# You can use: java -jar target/bcm-backend-*.jar com.donatodev.bcm_backend.util.JwtKeyGenerator
# Or online tool: https://www.allkeysgenerator.com/ (select 256-bit)

JWT_SECRET=your_base64_encoded_jwt_secret_key_min_256_bits
JWT_EXPIRATION_MS=86400000

# JWT_EXPIRATION_MS explained:
# 3600000    = 1 hour
# 86400000   = 24 hours (default)
# 604800000  = 7 days

# =================================================================
# FRONTEND CONFIGURATION
# =================================================================
# URL of your frontend application (used for CORS and email links)

FRONTEND_BASE_URL=http://localhost:3000

# For production:
# FRONTEND_BASE_URL=https://yourdomain.com

# =================================================================
# EMAIL CONFIGURATION
# =================================================================
# SMTP settings for sending emails (registration, password reset, etc.)
# Example shown for Gmail - adjust for your email provider

MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_specific_password

# For Gmail:
# 1. Enable 2-Factor Authentication
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character app password here (not your regular password)

MAIL_SMTP_AUTH=true
MAIL_SMTP_STARTTLS=true

# Other common providers:
# Outlook/Hotmail:
#   MAIL_HOST=smtp-mail.outlook.com
#   MAIL_PORT=587
#
# Yahoo:
#   MAIL_HOST=smtp.mail.yahoo.com
#   MAIL_PORT=587
#
# Custom SMTP:
#   MAIL_HOST=smtp.yourprovider.com
#   MAIL_PORT=587 (or 465 for SSL)

# =================================================================
# AWS CONFIGURATION (Optional - for future use)
# =================================================================
# Uncomment and configure when integrating AWS services

# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=eu-west-1
# AWS_S3_BUCKET_NAME=your-bucket-name

# =================================================================
# ADDITIONAL NOTES
# =================================================================
# 
# Development vs Production:
# - Use different databases for dev/prod
# - Use stronger passwords in production
# - Consider using environment-specific .env files:
#   .env.development, .env.production (all gitignored)
#
# Security Best Practices:
# - Never share your .env file
# - Use strong, unique passwords
# - Rotate JWT secrets periodically
# - Use different credentials for each environment
# 
# =================================================================