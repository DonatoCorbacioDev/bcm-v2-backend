# =================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# =================================================================

# Production Database Settings
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false

# Connection Pool - Production tuning
spring.datasource.hikari.maximum-pool-size=50
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.leak-detection-threshold=60000

# Logging - Production level
logging.level.com.donatodev.bcm_backend=INFO
logging.level.org.springframework=WARN
logging.level.org.springframework.security=WARN
logging.level.org.hibernate=WARN

# Log format and destination are owned by logback-spring.xml's "prod" profile
# (structured JSON to stdout), not by logging.pattern.console/logging.file.name
# properties — those were dead config here: a custom logback-spring.xml
# defines its own encoder per profile, which always wins over Boot's
# property-driven defaults. Stdout-only is intentional (12-factor): the
# container runtime/orchestrator owns log capture and rotation, not the app.

# Actuator - Limited endpoints for production
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=never
management.prometheus.metrics.export.enabled=true

# API docs - disabled in production (avoid exposing the full API surface publicly)
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# Security Settings
server.error.include-message=never
server.error.include-stacktrace=never
server.error.include-binding-errors=never

# Flyway production settings
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=false
spring.flyway.validate-on-migrate=true
spring.flyway.out-of-order=false

# Development Tools - Disabled in production
spring.devtools.restart.enabled=false
spring.devtools.livereload.enabled=false

# JWT expiration — driven by JWT_EXPIRATION_MS env var (default 24h from application.properties)

# CORS - Production (will use FRONTEND_BASE_URL from .env)
# Configured in CorsConfig.java